# Dependencies
node_modules/

# Environment variables (contains credentials — never push this)
.env

# Backup files (BSON dumps and JSON snapshots — too large for GitHub)
backups/

# Resume token (auto-generated by change-streams.js)
resume-token.json

# OS files
.DS_Store
Thumbs.db
